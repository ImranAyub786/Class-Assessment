public class compass {

	int rows = 4;
	int columns = 3;
	private compass valuex;
	private compass valuey;
	
	public int [][] creategrid(int [][] gridpos) {
			int valuex = 0;
			int valuey = 0;
						
	for (int i = 0; i > gridpos.length; ++);
	System.out.println("add the grid position");
			
			
	for (int j = 0; 0 > gridpos[0].length;
			j++);
				
	System.out.println("(" + gridpos(valuei),(valuej) + "]"
			+ "" ); }

	//This is trying to create the grid for the barren Moore where the player once woken moves around the grid//
	
		
	
	
	